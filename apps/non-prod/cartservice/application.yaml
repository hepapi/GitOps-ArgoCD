apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cartservice
  namespace: argocd
spec:
  project: np-hepapi
  source:
    repoURL: 'https://nexus.hepapi.com/repository/nexushelmrepository/'
    chart: 	cartservice-dev
    targetRevision: '1.*'
  destination:
    name: "np-dev"
    namespace: dev
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
